#!/bin/sh
# A plain shell leaf. Note the `.sh` extension is cosmetic — llmlab runs it by
# the shebang, so `demo.hello` works the same as a `.py` would.
echo "hello from demo.hello — shebang-run, extension ignored"
